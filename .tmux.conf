# Key bindings
	# Change prefix from ^B to ^A
	unbind C-b
	set-option -g prefix ^A

	# Send literal ^A
	bind-key ^A send-key ^A

	# Control nested Tmux
	bind-key a send-prefix

	# Reload configuration file
	bind-key r source-file $HOME/.tmux.conf

	# Lock current connected client
	bind-key L lock-client

# Server options
	# Start numbering at 1
	set-option -g base-index 1

	# Status message display time
	set-option -g display-time 2000

	# Allow faster key repetition
	set-option -s escape-time 0
	#set-option -sg escape-time 1

	# Set the maximum number of lines held in window history.
	set-option -g history-limit 5000

	# Lock with Cmatrix and BSD Lock
	set-option -g lock-command 'cmatrix -abs ; lock -np'

	# Copy text with mouse drag
	#set-option -g mode-mouse on

	# Resize panes with mouse drag
	set-option -g mouse-resize-pane on

	# Select pane with mouse click
	set-option -g mouse-select-pane on

	# Select window with mouse click
	set-option -g mouse-select-window on

	# Update status line
	set-option -g status-interval 1

# Window options
	# Automatically set content size to smallest connected terminal
	set-window-option -g aggressive-resize on

	# Start panes at 1 instead of 0. Tmux 1.6+
	set-window-option -g pane-base-index 1

	# Vi bindings in copy mode
	set-window-option -g mode-keys vi

	# Emacs bindings in command line
	set-window-option -g status-keys emacs

	# Xterm key sequences
	set-window-option -g xterm-keys on

	# Status line
	#set-window-option -g status off

# Colors
	# Workaround for Tmux race condition, check for *256col* and export $TERM=screen-256color with shell
	CONTAINING_TERM=$TERM

	# Check if terminal supports 256 colors
	if-shell 'test $(tput Co) -ge 256' 'set-option -g default-terminal "screen-256color"'

	# Good theme
	#set -g status-left ''
	#set -g window-status-format "#[fg=black,bg=colour148,bold,noreverse,nounderscore] #I #[bg=colour240] #[default]#[fg=colour231,bg=colour240,nobold]#W#[nounderscore] "
	#set -g window-status-current-format "#[fg=black,bg=colour231,bold] #I #[fg=colour231,bg=colour31,nobold] #W "
	#set -g status-right "#[fg=colour231,bg=colour236,bold] #H #[fg=colour250,bold] #S #[fg=white,bg=colour237,nobold] %a %H:%M"

	# If too few colors to identify alerts, show flag (currently doesn't work)
	#if-shell 'test $(tput Co) -ge 256' 'set-window-option -g window-status-format "#[bold,nounderscore] #I #[default]#[bg=colour237]#[nounderscore] #[default]#[fg=colour231,bg=colour237]#W#[nounderscore] "'
	#if-shell 'test $(tput Co) -lt 256' 'set-window-option -g window-status-format "#[bold,nounderscore] #I #[default]#[bg=colour237]#[nounderscore] #[default]#[fg=colour231,bg=colour237]#W#[nounderscore]#F "'

	#separator_left_bold="⮂"
	#separator_left_thin="⮃"
	#separator_right_bold="⮀"
	#separator_right_thin="⮁"

	set-window-option -g status-bg colour236

	set-window-option -g status-left-length 30
	#set-window-option -g status-left '#[bg=colour240,fg=white] #S '
	set-window-option -g status-left ''
	set-window-option -g window-status-format "#[bold,nounderscore] #I #[default]#[bg=colour240]#[nounderscore] #[default]#[fg=colour231,bg=colour240]#W#[nounderscore] "
	set-window-option -g window-status-current-format "#[bold] #I #[default]#[bg=colour117] #W "
	#set-window-option -g status-right "#[fg=colour231,bold] #H #[fg=colour250,bold]#[fg=white,bg=colour237,nobold] %a %H:%M "
	set-window-option -g status-right "#[fg=colour231,bold] #H #[fg=black,bg=colour252,bold] %a #[nobold]%H:%M "

	# Fancy characters
	#set-window-option -g status-left '#[bg=colour240,fg=white] #S #[fg=colour236,reverse]⮀'
	#set-window-option -g window-status-format "#[fg=colour236,nounderscore]⮀#[default,bold,nounderscore] #I #[fg=colour240,reverse]⮀#[default]#[bg=colour240]#[nounderscore] #[default]#[fg=colour231,bg=colour240]#W#[nounderscore] #[fg=colour236,reverse]⮀"
	#set-window-option -g window-status-current-format "#[fg=colour236]⮀#[bold] #I #[fg=colour117,reverse]⮀#[default]#[bg=colour117] #W #[fg=colour236,reverse]⮀"
	#set-window-option -g status-right "#[fg=colour231,bold] #H #[fg=colour237]⮂#[fg=white,bg=colour237,nobold] %a %H:%M "

	# Compact inactive windows
	#set-window-option -g window-status-format "#[fg=colour236,nounderscore]⮀#[default,bold,nounderscore] #I #[fg=colour236,reverse]⮀" #[default]#[bg=colour240]#[nounderscore] #[default]#[fg=colour231,bg=colour240]#W#[nounderscore] #[fg=colour236,reverse]⮀"
	# Compact current window
	#set-window-option -g window-status-current-format "#[fg=colour236]⮀#[default,fg=colour231,bold] #I #[default,fg=colour236,reverse]⮀"

	# Inactive
		# Black on green
		set-window-option -g window-status-fg black
		set-window-option -g window-status-bg colour76
		# Black on white
		#set-window-option -g window-status-bg colour231
		#set-window-option -g window-status-fg black
		# Green more alike non-256color
		#set-window-option -g window-status-bg colour40

	# Current
		set-window-option -g window-status-current-fg black
		set-window-option -g window-status-current-bg colour33
		set-window-option -g window-status-current-attr none

	# Activity/Silence
		set-window-option -g window-status-activity-fg black
		set-window-option -g window-status-activity-bg colour127
		set-window-option -g window-status-activity-attr bold,underscore

	# Bell
		set-window-option -g window-status-bell-fg black
		set-window-option -g window-status-bell-bg red # Red is urgent
		set-window-option -g window-status-bell-attr bold,underscore

	# Content
		set-window-option -g window-status-content-fg black
		set-window-option -g window-status-content-bg colour226 # Yellow because search hilighting
		set-window-option -g window-status-content-attr bold,underscore
